ask_ports_configured:
  &ask_ports_configured
  ask: ports_configured
  friendly: "Configs Good?"
  positive: done
  negative: done

when_pods_running:
  &when_pods_running
  ask: pods_accepting
  friendly: "Pods Connecting?"
  positive:
    *ask_ports_configured
  negative:
    ask: image_accepting
    friendly: "Container Connecting?"
    positive:
      *ask_ports_configured
    negative: done

tree:
  ask: can_connect
  friendly: "Service Connects?"
  positive: done
  negative:
    ask: pods_in_svc
    friendly: "Service Sees Pods?"
    positive:
      *when_pods_running
    negative:
      ask: pods_running
      friendly: "Pods Running?"
      positive:
        *when_pods_running
      negative: done
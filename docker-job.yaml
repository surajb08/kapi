apiVersion: batch/v1
kind: Job
metadata:
  name: docker-test

spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: main
          image: docker:latest
          command: ["docker",  "image", "ls"]
          env:
            - name: DOCKER_HOST
              value: tcp://10.56.1.39:2375
